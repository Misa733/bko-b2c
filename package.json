{
  "name": "cockpit-comercial-b2c",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema interno para consolidacao de planilhas comerciais B2C.",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
